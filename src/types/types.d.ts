export type EmailConfigType = {
  host: string;
  port: number;
  user: string;
  email: string;
  password: string;
};


export type ModalType = {
  title: string;
  body: string;
  isOpen: boolean;
}